% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_color_stern_div}
\alias{scale_color_stern_div}
\alias{scale_fill_stern_div}
\title{Stern diverging color scale.}
\usage{
scale_color_stern_div(..., midpoint = 0, limits = NULL, reverse = FALSE)

scale_fill_stern_div(..., midpoint = 0, limits = NULL, reverse = FALSE)
}
\arguments{
\item{...}{Passed to the underlying ggplot2 scale.}

\item{midpoint}{Numeric. The data value mapped to the neutral midpoint.
Default 0. Pass the chart's true midpoint (e.g. citywide average).}

\item{limits}{Numeric vector of length 2. Force symmetric limits if
needed. Default \code{NULL} (auto-computed).}

\item{reverse}{Logical. If \code{TRUE}, navy maps to positive and olive to
negative. Default \code{FALSE}.}
}
\value{
A ggplot2 scale.
}
\description{
Olive <-> navy ramp with a warm cream midpoint (see \link{stern_div}). Pass
\verb{midpoint =} to set where neutral falls in your data.
}
\details{
Pass \code{reverse = TRUE} when "up" means "bad" for the metric (e.g.
crime, eviction) -- the convention is that olive should always read as
positive in the metric being shown.
}
\examples{
\dontrun{
df <- data.frame(
  x = letters[1:10],
  pct_change = c(-12, -8, -3, -1, 0, 2, 4, 6, 9, 14)
)
ggplot(df, aes(x, pct_change, fill = pct_change)) +
  geom_col() +
  scale_fill_stern_div(midpoint = 0) +
  theme_stern()
}

}
