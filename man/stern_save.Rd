% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{stern_save}
\alias{stern_save}
\title{Save a ggplot with sensible defaults.}
\usage{
stern_save(
  plot,
  filename,
  size = "default",
  width = NULL,
  height = NULL,
  dpi = 300,
  bg = NULL,
  ...
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{filename}{Output file path. Format inferred from extension.}

\item{size}{Named preset for dimensions. One of \code{"small"},
\code{"default"}, \code{"wide"}, \code{"square"}, \code{"social"}, \code{"poster"}. See the
\emph{Size presets} section. Ignored if both \code{width} and \code{height} are
supplied.}

\item{width, height}{Explicit dimensions in pixels. Override \code{size}.}

\item{dpi}{Resolution. Default 300 (print quality). Use 144 for
web-only output to halve file size with no visible loss on
non-retina displays.}

\item{bg}{Background color. Default \link{stern_bg_primary}. Pass
\code{"white"} for a white background, \code{NA} for transparent.}

\item{...}{Additional arguments forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}},
e.g. \code{device = ragg::agg_png} if you want to force a specific
graphics backend.}
}
\value{
Invisibly, the filename.
}
\description{
A streamlined alternative to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that handles the
three common gotchas:
}
\details{
\enumerate{
\item \strong{Pixel dimensions, not inches.} You think "1600 px wide", not
"5.33 inches wide at 300 dpi".
\item \strong{\code{showtext} DPI is auto-synced to the export DPI.} Without this,
saving at 300 dpi while \code{showtext} is set up at screen DPI (96)
renders text at ~32\% of the correct size -- the classic "why did
my titles shrink" bug.
\item \strong{Background defaults to the Stern cream}, so saved plots match
the on-screen theme. Override with \code{bg = "white"} or \code{bg = NA}.
}

Format is detected from the filename extension (\code{.png}, \code{.pdf},
\code{.svg}, \code{.jpg}).
}
\section{Size presets}{

\tabular{lll}{
\strong{Preset}  \tab \strong{Pixels}     \tab \strong{Use case} \cr
\code{small}     \tab 1200 x 750     \tab Slack messages, blog post bodies \cr
\code{default}   \tab 1600 x 1000    \tab Reports, decks, the everyday case \cr
\code{wide}      \tab 2000 x 1000    \tab Wide editorial layouts \cr
\code{square}    \tab 1200 x 1200    \tab Instagram, square cards \cr
\code{social}    \tab 1200 x 630     \tab Twitter cards, og:image \cr
\code{poster}    \tab 2400 x 1500    \tab High-resolution print \cr
}
}

\examples{
\dontrun{
library(ggplot2)
library(stern)

stern_setup_fonts()

p <- ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) +
  geom_point(size = 3) +
  scale_color_stern_cat() +
  labs(title = "Fuel economy") +
  theme_stern()

# Most common case -- just save it
stern_save(p, "fuel-economy.png")

# Pick a different shape via preset
stern_save(p, "fuel-economy-wide.png", size = "wide")

# Or specify dimensions in pixels
stern_save(p, "fuel-economy-square.png", width = 1080, height = 1080)

# PDF for vector output (dpi only affects raster fallback)
stern_save(p, "fuel-economy.pdf")

# White background instead of cream
stern_save(p, "fuel-economy-white.png", bg = "white")
}

}
