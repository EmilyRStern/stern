% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-fonts.R
\name{stern_setup_fonts}
\alias{stern_setup_fonts}
\title{Register fonts used by \code{theme_stern()}.}
\usage{
stern_setup_fonts(dpi = 96, bold_weight = 600, regular_weight = 400)
}
\arguments{
\item{dpi}{Numeric. Resolution for \code{showtext} rendering. Default 96.
Bump to 150-300 for high-resolution exports via \code{ggsave()}.}

\item{bold_weight}{Numeric. The weight to use for \code{face = "bold"} text
in the registered families. Default 600 ("semibold") for the
editorial half-bold look that pairs with the Shiny CSS. Use 700 for
true bold, 500 for medium.}

\item{regular_weight}{Numeric. The weight for \code{face = "plain"} (body)
text. Default 400.}
}
\value{
Invisibly \code{TRUE} on success.
}
\description{
Registers Source Serif 4 (used for headlines and body copy) and Source
Sans 3 (used for axis labels, legends, captions, and UI) via Google
Fonts, and turns on \code{showtext} rendering for the current session.
}
\details{
Call once at the start of any script or .Rmd that uses the theme.
Requires the optional packages \code{showtext} and \code{sysfonts}.
}
\section{Font weights}{

Both Source Serif 4 and Source Sans 3 are variable fonts, so any
weight from 200 (extralight) to 900 (black) is available. Common
picks:
\tabular{ll}{
400 \tab Regular -- body text \cr
500 \tab Medium -- subtle emphasis \cr
600 \tab Semibold -- editorial "half bold," default for \code{face = "bold"} \cr
700 \tab Bold -- traditional heavy weight \cr
}
}

\examples{
\dontrun{
library(ggplot2)
library(stern)

stern_setup_fonts()                       # semibold for face = "bold"
stern_setup_fonts(bold_weight = 700)      # full bold
stern_setup_fonts(bold_weight = 500)      # even lighter "bold"

ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  labs(title = "Fuel economy", caption = "Source: mtcars") +
  theme_stern()
}

}
