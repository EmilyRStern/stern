% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test.R
\name{stern_test_shiny}
\alias{stern_test_shiny}
\title{Launch the Shiny test app.}
\usage{
stern_test_shiny()
}
\value{
The return value of \code{\link[shiny:runApp]{shiny::runApp()}} (invisibly). The app
blocks the R session until the browser tab is closed or the user
interrupts.
}
\description{
Runs a small Shiny app that exercises every component styled by the
\code{stern} Shiny module: the \code{stern_app_shell()} layout, sidebar form
inputs, cards, \code{stern_stat_callout()} value boxes, tabs, a \code{DT} data
table, and \code{highcharter} charts wired up through \code{hc_theme_stern()}.
Use it after install to confirm the Shiny styling renders correctly.
}
\details{
Requires the optional packages \code{shiny}, \code{bslib}, \code{htmltools}, \code{DT},
\code{highcharter}, \code{dplyr}, and \code{tibble}, plus \code{showtext} and \code{sysfonts}
for the fonts.
}
\examples{
\dontrun{
library(stern)
stern_test_shiny()
}

}
