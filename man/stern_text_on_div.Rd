% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-text.R
\name{stern_text_on_div}
\alias{stern_text_on_div}
\title{Pick text color based on a diverging fill value.}
\usage{
stern_text_on_div(values, midpoint = 0, flip_distance = NULL)
}
\arguments{
\item{values}{Numeric vector -- the fill values underneath the labels.}

\item{midpoint}{Numeric. The neutral midpoint. Default 0.}

\item{flip_distance}{Numeric. How far from the midpoint before text
flips to cream. Defaults to half the maximum absolute deviation,
which puts the flip at roughly the boundary between the cream-neutral
band and the first colored band.}
}
\value{
A character vector of hex colors.
}
\description{
Both ends of the diverging scale are dark, so text needs to flip to
cream on both sides while staying dark in the middle.
}
\examples{
\dontrun{
library(ggplot2)

ggplot(df, aes(x, y, fill = pct_change)) +
  geom_tile() +
  geom_text(
    aes(label = pct_change, color = stern_text_on_div(pct_change)),
    family = "stern_sans"
  ) +
  scale_fill_stern_div(midpoint = 0) +
  scale_color_identity()
}

}
