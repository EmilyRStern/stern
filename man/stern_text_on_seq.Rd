% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-text.R
\name{stern_text_on_seq}
\alias{stern_text_on_seq}
\title{Pick text color based on a sequential fill value.}
\usage{
stern_text_on_seq(values, threshold = NULL)
}
\arguments{
\item{values}{Numeric vector -- the fill values underneath the labels.}

\item{threshold}{Numeric. The cutoff above which text flips to cream.
Defaults to the midpoint of \code{values}. Override when your scale is
asymmetric (e.g. fill values 0-100 but most data is 60-95).}
}
\value{
A character vector of hex colors, same length as \code{values}.
}
\description{
Returns dark text for light fills and cream text for dark fills, with
a threshold at the midpoint of \code{values} by default. Use inside
\code{geom_text()} to label heatmap or tile cells without losing labels on
the dark end of the ramp.
}
\details{
Pair with \code{ggplot2::scale_color_identity()} so ggplot uses the colors
as-is.
}
\examples{
\dontrun{
library(ggplot2)

ggplot(df, aes(x, y, fill = score)) +
  geom_tile() +
  geom_text(
    aes(label = score, color = stern_text_on_seq(score)),
    family = "stern_sans"
  ) +
  scale_fill_stern_seq() +
  scale_color_identity()
}

}
