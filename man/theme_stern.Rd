% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-ggplot.R
\name{theme_stern}
\alias{theme_stern}
\title{Stern theme for ggplot2.}
\usage{
theme_stern(base_size = 14, panel_bg = stern_bg_surface, legend = "bottom")
}
\arguments{
\item{base_size}{Numeric. Base font size in points. Default 12.}

\item{panel_bg}{Character. Plot panel background. Default white
(\link{stern_bg_surface}). Pass \link{stern_bg_primary} for a "no panel" look
that merges into the page -- rare; most charts want the white panel
for contrast against the cream page.}

\item{legend}{Character. Legend position. One of \code{"bottom"} (default),
\code{"top"}, \code{"left"}, \code{"right"}, or \code{"none"}. Also accepts a numeric
\code{c(x, y)} for an inset legend.}
}
\value{
A \code{ggplot2::theme} object.
}
\description{
Editorial civic-data styling: cream background, white plot panel, soft
tan gridlines, Source Serif 4 headlines, Source Sans 3 labels and axes.
}
\details{
Call \code{\link[=stern_setup_fonts]{stern_setup_fonts()}} once before plotting so the typefaces are
available.
}
\examples{
\dontrun{
library(ggplot2)
stern_setup_fonts()

ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) +
  geom_point(size = 3) +
  scale_color_stern_cat() +
  labs(
    title    = "Heavier cars use more fuel",
    subtitle = "Weight vs. mpg in the mtcars dataset",
    caption  = "Source: 1974 Motor Trend"
  ) +
  theme_stern()

# Move the legend (or hide it) per chart
last_plot() + theme_stern(legend = "right")
last_plot() + theme_stern(legend = "none")
}

}
